#!/usr/bin/env groovy

node(jenkins_slave) {
    stage ('checkout code from git')
    
    
    stage ("Build")
        sh "mvn -version"
        sh "java -version"
        sh "docker version" 
    }
  
